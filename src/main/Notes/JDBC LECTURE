-So instead of creating the objects directly and locally like we have been so far w adlister, we are going to learn how to import from a database

CREATE DATABASE IF NOT EXISTS books_example;
USE books example;
CREATE TABLE books(
    id int UNSIGNED AUTO_INCREMENT,
    title VARCHAR(255) NOT NULL,
    author VARCHAR(255) NOT NULL,
    PRIMARY KEY (id)
    );

INSERT INTO books(title, author) VALUES ('The Hunchback of Notre Dame', 'Victor Hugo'), ('Green Eggs and Ham', 'Dr. Seuss');

<dependency>
    <groupId>mysql</groupId>
    <artifactId>mysql-connector-java</artifactId>
    <version>8.0.32</version>
</dependency>
















